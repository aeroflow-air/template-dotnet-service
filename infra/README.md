# Infrastructure

Bicep / Azure Verified Modules (AVM) for this service will land here.

Nothing is checked in yet on purpose — avoid inventing placeholder `.bicep` that drifts from platform standards. When the platform landing zone and AVM patterns are ready, add modules under this folder and document the deploy path in the service README.
